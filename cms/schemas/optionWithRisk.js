import { NAME_FIELD, RISK_WEIGHTING_FIELD } from './baseOptionFields';

export default {
    name: 'optionWithRisk',
    type: 'object',
    fields: [
        NAME_FIELD,
        RISK_WEIGHTING_FIELD
    ]
}