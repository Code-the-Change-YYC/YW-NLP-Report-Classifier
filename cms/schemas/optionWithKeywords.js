import { NAME_FIELD, KEYWORDS_FIELD } from './baseOptionFields';

export default {
    name: 'optionWithKeywords',
    type: 'object',
    fields: [
        NAME_FIELD,
        KEYWORDS_FIELD
    ]
}
